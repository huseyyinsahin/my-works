export type instructorProps = {
    instructorNameLastName: {
      firstName: string;
      lastName: string;
    };
  };

 export type DateComing = {
    name: string;
    courseNumber: number;
    isBest: boolean;
    
  };